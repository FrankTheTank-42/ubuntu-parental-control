name: Tests

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  browser-tests:
    name: Browser tests (Node.js)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          package-manager-cache: false
      - name: Check options-page syntax
        run: node --check browser-extension/options/options.js
      - name: Run JavaScript tests
        shell: bash
        run: |
          set -euo pipefail
          for test_file in tests/*.js; do
            node "$test_file"
          done

  python-tests:
    name: Python tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Check Python syntax
        run: python -m compileall -q daemon installer tools tests
      - name: Run Python tests
        run: python -m unittest discover -s tests -p 'test_*.py'

  installer-and-build:
    name: Installer and extension packages
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Install installer test dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends --yes pkexec
      - name: Run installer integration test
        run: bash tests/test-installer.sh
      - name: Build Firefox and Chrome packages
        run: python tools/build_extension.py
      - name: Upload extension packages
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: browser-extension-packages
          path: |
            dist/ubuntu-parental-control-webfilter-firefox-unsigned.xpi
            dist/ubuntu-parental-control-webfilter-chrome.zip
          if-no-files-found: error
          retention-days: 14
